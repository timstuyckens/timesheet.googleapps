#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jade/bin/grunt-jade" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jade/bin/grunt-jade" "$@"
  ret=$?
fi
exit $ret
